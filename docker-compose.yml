services:
  php:
    build: .
    security_opt:
      - apparmor:unconfined
    volumes:
      - ./:/app:delegated
    ports:
      - '8000:80'
    env_file:
      - .env
    environment:
      MONGO_HOST: mongo
      MONGO_PORT: 27017

networks:
  default:
    external:
      name: ic_markov_net
